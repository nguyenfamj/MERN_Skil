export interface loginAuthResponse {
  success: boolean;
  message: string;
  accessToken: string;
}
export interface registerAuthResponse {
  success: boolean;
  message: string;
  accessToken: string;
}
